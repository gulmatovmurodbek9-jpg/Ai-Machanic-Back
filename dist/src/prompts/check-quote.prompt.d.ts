export declare const buildCheckQuotePrompt: (carYear: number, carMake: string, carModel: string, problem: string, diagnosis: string, quotedAmount: number, estimatedMin: number, estimatedMax: number) => string;
export declare const CHECK_QUOTE_PROMPT: string;
